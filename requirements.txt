# Core ML
torch>=2.5.0
transformers>=4.51.0
datasets>=4.0.0
vllm>=0.7.0

# Embedding & Clustering
faiss-gpu-cu12

# Utilities
json_repair
pyyaml
tqdm
numpy
pandas

# Fine-tuning (used via open-instruct, not by the selection pipeline)
# deepspeed>=0.17.0
# accelerate>=1.6.0
# peft>=0.17.0
# bitsandbytes>=0.41.1
